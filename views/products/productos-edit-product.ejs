<!DOCTYPE html>
<html lang="en">
  <head>
    <!--HEAD -->
    <%- include('../partials/head', {title: 'Editar los productos'}) %>
  </head>
  <body>
    <!--HEADER -->
    <%- include('../partials/header') %>

    <!--MAIN -->
    <main>
      <!--agregar un input para hacer busquedas por categoria-->
      <div class="mlr-5 mt-3">
        <label for="categoriaList" class="form-label"
        >Categorias de productos</label
        >
        <input
          class="form-control"
          list="opciones"
          id="categoriaList"
          placeholder="Escribe para buscar..."
        />

      </div>
      <!-- se listan todos los productos-->
      <div class="container products-wrapper overflow-hidden">
        <!--row para titulo-->
        <div class="row">
          <div class="col-12">
            <h2 class="products-title align-center">
              Estas editando: <i> <%= productoEditar.nombre%> </i>
            </h2>
          </div>
          <form action="/products/edit/<%= productoEditar.id %>?_method=PUT " 
                method="POST"
                class="form-field">
            <div class="row product-detail">
                <div class = "mt-5 w-50 mlr-5 ">
                    <label for="name" class="form-label">Nombre del producto:</label>
                    <input
                        value="<%=productoEditar.nombre%> "
                        type="text"
                        id="name"
                        name="name"
                        placeholder="Ej: Mueble nordico"
                        class="form-control"
                        />
                </div>
                <div class = "mt-5 w-50 mlr-5 ">
                    <label for="name" class="form-label">Categoria:</label>
                    <input
                    value="<%=productoEditar.categoria%>"
                    class="form-control"
                    list="opciones"
                    id="categoriaList"
                    placeholder="Escribe para buscar..."
                    />
                </div>
                <div class = "mt-5 w-50 mlr-5">
                  <label for="name" class="form-label">Ambiente:</label>
                  <input
                  value="<%=productoEditar.ambiente%>"
                  class="form-control"
                  list="opciones"
                  id="categoriaList"
                  placeholder="Escribe para buscar..."
                />
                </div>
                <div class = "mt-5 w-50 mlr-5 ">
                  <label for="name" class="form-label">Precio al contado:</label>
                  <input
                  value="<%=productoEditar.precioContado%>"
                  class="form-control"
                  list="opciones"
                  id="categoriaList"
                  placeholder="Escribe para buscar..."
                />
                </div>
                <div class = "mt-5 w-50 mlr-5 ">
                  <label for="name" class="form-label">Cantidad de cuotas:</label>
                  <input
                  value="<%=productoEditar.cantidadDeCuotas%>"
                  class="form-control"
                  list="opciones"
                  id="categoriaList"
                  placeholder="Escribe para buscar..."
                />
                </div>
                <div class = "mt-5 w-50 mlr-5 ">
                  <label for="name" class="form-label">Precio por cada cuotas:</label>
                  <input
                  value="<%=productoEditar.precioCuotas%>"
                  class="form-control"
                  list="opciones"
                  id="categoriaList"
                  placeholder="Escribe para buscar..."
                />
                </div>
                <div class = "mt-5 w-50 mlr-5 ">
                  <label for="name" class="form-label">Envio gratis:</label>
                  <input
                  value="<%=productoEditar.envioGratis%>"
                  class="form-control"
                  list="opciones"
                  id="categoriaList"
                  placeholder="Escribe para buscar..."
                />
                </div>
                <div class = "mt-5 w-50 mlr-5 ">
                  <label for="name" class="form-label">Alternativo:</label>
                  <input
                  value="<%=productoEditar.alt%>"
                  class="form-control"
                  list="opciones"
                  id="categoriaList"
                  placeholder="Escribe para buscar..."
                />
                </div>
                <div class = "mt-5 w-50 mlr-5 ">
                  <label for="name" class="form-label">Descripcion:</label>
                  <input
                  value="<%=productoEditar.descripcion%>"
                  class="form-control"
                  list="opciones"
                  id="categoriaList"
                  placeholder="Escribe para buscar..."
                />
                </div>
                <div class = "mt-5 w-50 mlr-5 ">
                  <label for="name" class="form-label">Medidas:</label>
                  <input
                  value="<%=productoEditar.medidas%>"
                  class="form-control"
                  list="opciones"
                  id="categoriaList"
                  placeholder="Escribe para buscar..."
                />
                </div>
                <div class = "mt-5 w-50 mlr-5 ">
                  <label for="name" class="form-label">Color:</label>
                  <input
                  value="<%=productoEditar.color%>"
                  class="form-control"
                  list="opciones"
                  id="categoriaList"
                  placeholder="Escribe para buscar..."
                />
                </div>
                <div class = "mt-5 w-50 mlr-5 ">
                  <label for="name" class="form-label">Color:</label>
                  <input
                  value="<%=productoEditar.color%>"
                  class="form-control"
                  list="opciones"
                  id="categoriaList"
                  placeholder="Escribe para buscar..."
                />
                </div>
                <div class = "mt-5 w-50 mlr-5 ">
                  <label for="description" class="form-label">Detalles del producto:</label>
                    <textarea
                  value="<%=productoEditar.detalles%>"
                  class="form-control"
                  name="description"
                  id="description"></textarea>
                <div>
                <button type="submit" class="btn btn-outline-primary btn-lg product-btn">
                        Guardar producto
                </button>
                </div>
            </div>
          </form>
        </div>
        <!-- fin del row -->
      </div>
    </main>

    <!--FOOTER -->
    <%- include('../partials/footer') %>
  </body>
</html>

