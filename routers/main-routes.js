const path = require('path')
const express = require('express')
const router = express.Router();

router.get("/", main-controllers.home) 
router.get("/###", main-controllers.###)

module.exports = router;